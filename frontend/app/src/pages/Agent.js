import AddAgent from '../Agent/AddAgent'
import ListAgent from '../Agent/ListAgent';

const Agent = () => {
    return (
        <div>
            <h1>Agent</h1>
            <ListAgent />
            <AddAgent />
        </div>
    );
};

export default Agent;